spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

docconv:
  pandoc:
    path: pandoc
    timeout-seconds: 30
  storage:
    temp-dir: ${java.io.tmpdir}/docconv
